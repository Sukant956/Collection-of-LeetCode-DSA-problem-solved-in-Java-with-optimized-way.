// Problem: Insert Intervals
// Platform: Leetcode and Striver
// Approach:3-step interval processing approach
// Time Complexity:O(n)
// Space Complexity:O(n)

// Code 

class Solution {
    public int[][] insert(int[][] intervals, int[] newInterval) {
        int result[][] = new int [intervals.length +1][2];
        int j =0,i=0;

        // Adding all intervals thats ends before new intervals
        while(i < intervals.length && intervals[i][1] < newInterval[0]){
            result[j++] = intervals[i++];
        }
        // Merge Overlap interval
        while(i < intervals.length && intervals[i][0] <= newInterval[1]){
            newInterval[0] = Math.min(newInterval[0],intervals[i][0]);
            newInterval[1] = Math.max(newInterval[1],intervals[i++][1]);
        }
        result[j++] = newInterval;

        // Add Remaning
        while(i < intervals.length){
            result[j++] = intervals[i++];
        }
        return java.util.Arrays.copyOf(result,j);
        
    }
}
