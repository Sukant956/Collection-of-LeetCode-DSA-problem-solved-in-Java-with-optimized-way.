// Problem: Largest Odd Number in String
// Platform: LeetCode and Striver
// Approach: Right-to-Left Linear Scan
// Time Complexity: O(n)
// Space Complexity: O(n) or O(1)

// Code

class Solution {
    public String largestOddNumber(String num) {
        int length = num.length() - 1; // Finding the length of the given String.
        while(length >= 0)
        {
            int d = num.charAt(length) -'0'; // if we do not substract zero from it then it will return the asky value of that number, substracting number will return as it is
            if(d % 2 != 0)
            {
                return num.substring(0,length + 1);
            }
            length --;
        }
        return "";
    }
}
